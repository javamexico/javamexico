<html t:type="layout" title="JavaMexico :: Preguntas :: ${pregunta.titulo}"
      t:sidebarTitle="Preguntas similares"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd"
      xmlns:p="tapestry:parameter">
        <!-- Most of the page content, including <head>, <body>, etc. tags, comes from Layout.tml -->

	<h2>${pregunta.titulo}</h2>
	<cite>${pregunta.autor.nombre}</cite>
    <p>${pregunta.pregunta}</p>
    Tags: - <t:loop t:source="pregunta.tags" t:value="tag">
    	${tag.tag} -
    </t:loop>

	<h2>Respuestas:</h2>
	<ul><t:loop t:source="respuestas" t:value="resp" t:element="li">
		${resp.respuesta}
		<br/>
		por ${resp.autor.nombre} el ${resp.fecha}
	</t:loop></ul>

	<t:ligausuario text="Agregar una respuesta">
	<t:form t:id="respform" context="${pregunta.pid}">
	<t:textarea t:value="resptext" cols="40" rows="10" />
	<br/><t:submit value="Contestar" />
	</t:form></t:ligausuario>


    <p:sidebar>
		Aqui podriamos poner preguntas similares (la similaridad la determinamos por los tags, etc)
    </p:sidebar>

</html>
